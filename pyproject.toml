[project]
name = "pqc-tls-scanner"
version = "0.1.0"
description = "TLS scanner for PQC (ML-KEM) group detection"
readme = "README.md"
authors = [
    { name = "jhonnjc15", email = "jhonatanjcca@gmail.com" }
]
requires-python = ">=3.15"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"